# lion-vs-tiger-image-classification
A computer vision project that uses a Convolutional Neural Network (CNN) to classify images as Lion or Tiger using Python, TensorFlow, and # Lion vs Tiger Image Classification

This project uses a Convolutional Neural Network (CNN) to classify images as Lion or Tiger.

## Technologies
Python, TensorFlow, OpenCV

## How to Run
1. Install requirements
2. Run train.py
3. Run predict.py

## Output
Prediction result is saved in output folder.

